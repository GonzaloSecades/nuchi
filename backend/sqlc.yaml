version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/db/queries/"
    schema: "migrations/"
    gen:
      go:
        package: "dbgen"
        out: "internal/db/gen/"
        sql_package: "pgx/v5"
        emit_json_tags: false
